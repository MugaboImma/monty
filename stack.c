#include "monty.h"
/**
 * func_stack - An opcode that sets the format of the data to a stack (LIFO)
 * @head: the stack head
 * @count: the line_num
 * Return: no return
 */
void func_stack(stack_t **head, unsigned int count)
{
	(void)head;
	(void)count;
	bus.lifi = 0;
}
